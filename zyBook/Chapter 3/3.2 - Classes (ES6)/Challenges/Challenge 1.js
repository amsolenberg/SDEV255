// Create a Dog class with a constructor that takes two arguments and
// initializes properties name and gender in that order.

class Dog {
    constructor(name, gender) {
        this.name = name;
        this.gender = gender;
    }
}