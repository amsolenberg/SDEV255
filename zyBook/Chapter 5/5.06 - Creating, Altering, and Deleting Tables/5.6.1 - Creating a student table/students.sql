CREATE TABLE
    student (
        stu_id INTEGER,
        last_name VARCHAR(50),
        first_name VARCHAR(50),
        birth_date DATE,
        gpa DECIMAL(2, 1),
        PRIMARY KEY (stu_id)
    );